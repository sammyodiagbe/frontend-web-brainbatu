
"use client"
import { Button } from "@/components/ui/button";

const CreateAccountComponent = () => {

    
  return <Button onClick={() => {}}>Setup Bank</Button>;
};

export default CreateAccountComponent;
